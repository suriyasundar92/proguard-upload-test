/*
pluginManagement {
    resolutionStrategy {
        eachPlugin {
            if (requested.id.namespace == 'com.flurry.android') {
                useModule('com.flurry:symbol-upload:1.3.0')
            }
        }
    }
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}
*/
include ':app'
